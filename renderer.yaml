services:
  - type: web
    name: b2b
    env: static
    routes:
      - type: rewrite
        source: /api/*
        destination: https://backend-3mvr.onrender.com/api/*
      - type: redirect
        source: /*
        destination: /index.html
        status: 200